apiVersion: apps/v1
kind: Deployment
metadata:
  name: vasa-sla-monitor
spec:
  replicas: 1
  selector:
    matchLabels: { app: vasa-sla-monitor }
  template:
    metadata:
      labels: { app: vasa-sla-monitor }
    spec:
      serviceAccountName: vasa-worker
      containers:
        - name: worker
          image: vasa-eos-se-worker:latest
          command: ["node", "--experimental-transform-types", "--import", "./scripts/worker-register.mjs", "scripts/worker-main.ts", "sla-monitor"]
          envFrom: [{ secretRef: { name: vasa-runtime-secrets } }]
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          livenessProbe: { httpGet: { path: /health, port: 3001 }, initialDelaySeconds: 20, periodSeconds: 30 }
          readinessProbe: { httpGet: { path: /health, port: 3001 }, initialDelaySeconds: 10, periodSeconds: 15 }
