apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-platformd
spec:
  selector: { app: {{ .Release.Name }}-platformd }
  ports: [ { port: {{ .Values.service.port }}, targetPort: http, name: http } ]
